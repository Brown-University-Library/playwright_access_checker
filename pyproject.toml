[project]
name = "playwright-access-checker"
version = "0.0.0"
description = "Starter code for a planned Playwright-based access checker."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "httpx~=0.28.0",
    "python-dotenv~=1.2.0",
    "trio~=0.32.0",
]

[dependency-groups]
local = []
staging = []
prod = []
