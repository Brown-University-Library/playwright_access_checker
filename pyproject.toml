[project]
name = "playwright-access-checker"
version = "0.0.0"
description = "Records repeatable Studio browsing trials and access interference."
readme = "README.md"
requires-python = "==3.12.14"
dependencies = [
    "playwright>=1.55,<2",
    "python-dotenv~=1.2.0",
]

[dependency-groups]
local = []
staging = []
prod = []
