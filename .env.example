# Copy to ../.env only if that file does not already exist.
# CLI options override environment variables, then ../.env, then defaults.
COLLECTION_ID="bdr:nz9qn2kb"
WORKFLOW="tabs"
CF_SETTINGS_LABEL="unknown"
CF_SETTINGS_NOTES="Current rules have not been supplied; do not infer a Cloudflare request limit."
CF_SETTINGS_SINCE="unknown"
NETWORK_LABEL="unknown"
PUBLIC_IP="unknown"
IP_NOTES="Public IP, recent use, and time since any previous block have not been verified."
OPEN_INTERVAL_SECONDS=1.0
OPEN_JITTER_SECONDS=0.1
VIEW_SECONDS=5.0
VIEW_JITTER_SECONDS=0.5
SELECTION_START=1
SEED=42
MAX_DURATION_SECONDS=300
MAX_ITEMS=20
MAX_SCROLL_ACTIONS=20
NAVIGATION_TIMEOUT_SECONDS=30
BDR_HOSTS="repository.library.brown.edu"
OUTPUT_DIR="../runs"
# Optional, fixed proxy prepared before the trial; include an explicit port.
# PROXY_SERVER="http://proxy.example.org:8080"
# Supply PROXY_USERNAME and PROXY_PASSWORD separately when needed; never share them.
